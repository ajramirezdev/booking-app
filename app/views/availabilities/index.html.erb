<% provide(:title, 'Availabilities') %>
<h1 class="text-6xl font-bold text-center py-8">Availabilities</h1>
<div class="text-center">
  <div class="mb-4"><%= link_to "Add Availabilities", new_availability_path, class: "bg-emerald-900 text-white font-bold px-4 py-2 rounded" %></div>
  <%= paginate @availabilities %>
</div>
<ul class="flex flex-col gap-4 px-24">
  <%= render partial: 'availability', collection: @availabilities %>
</ul>
