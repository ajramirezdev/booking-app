<% provide(:title, "Home") %>
<div class="flex flex-col items-center mt-12 px-20">
  <div class="max-w-screen-xl w-full">
    <div class="flex justify-end mb-8">
      <%= link_to "Add Timeslot", new_time_slot_path, class: "bg-emerald-900 text-white font-bold px-4 py-2 rounded" %>
    </div>
    <div>
      <%= render partial: 'time_slot', collection: @time_slots %>
    </div>
  </div>
</div>
